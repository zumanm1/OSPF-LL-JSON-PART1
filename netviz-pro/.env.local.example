# NetViz Pro Environment Configuration
# Copy this file to .env.local and fill in your values

# ==============================================================================
# SECURITY CRITICAL - REQUIRED SETTINGS
# ==============================================================================

# JWT Secret Key (REQUIRED - Must be 32+ characters, random, unique)
# Generate with: openssl rand -base64 32
# DO NOT use placeholder values in production!
APP_SECRET_KEY=YOUR_STRONG_RANDOM_SECRET_KEY_HERE_MINIMUM_32_CHARS

# Admin Reset PIN (REQUIRED - Must be 8+ characters, NOT a common PIN)
# Use a strong, unique value (NOT 12345, 00000, 11111, etc.)
# Example: openssl rand -hex 8
ADMIN_RESET_PIN=YOUR_STRONG_PIN_HERE_8PLUS_CHARS

# ==============================================================================
# ADMIN ACCOUNT - REQUIRED
# ==============================================================================

# Admin Username (minimum 3 characters)
APP_ADMIN_USERNAME=netviz_admin

# Admin Password (REQUIRED)
# Must be 12+ characters with:
# - Upper and lowercase letters
# - Numbers
# - Special characters
# - NOT contain common words like 'admin', 'password', 'change'
APP_ADMIN_PASSWORD=YOUR_SECURE_ADMIN_PASSWORD_HERE

# ==============================================================================
# SERVER CONFIGURATION
# ==============================================================================

# Auth Server Port
AUTH_PORT=9041

# Gateway Port (public-facing - this is the main entry point)
GATEWAY_PORT=9040

# Vite Internal Port (localhost only, proxied by gateway)
VITE_INTERNAL_PORT=9042

# Session Timeout (in seconds, default: 3600 = 1 hour)
APP_SESSION_TIMEOUT=3600

# Server Binding - Controls which interface the server listens on
# Options: 127.0.0.1 (localhost only), 0.0.0.0 (all interfaces), or specific IP
# Using 0.0.0.0 to allow external access - IP whitelist controls who can connect
SERVER_HOST=0.0.0.0

# Localhost Only Mode (overrides SERVER_HOST if true)
# true = only accessible from localhost (127.0.0.1)
# false = use SERVER_HOST setting
LOCALHOST_ONLY=false

# ==============================================================================
# IP ACCESS CONTROL
# ==============================================================================

# IP Whitelist - Comma-separated list of allowed client IPs
# Use 0.0.0.0 to allow all IPs (not recommended for production)
# Examples: 
#   ALLOWED_IPS=127.0.0.1                              # localhost only
#   ALLOWED_IPS=127.0.0.1,192.168.1.0/24              # localhost + local subnet
#   ALLOWED_IPS=127.0.0.1,192.168.1.100,10.0.0.50    # specific IPs
#   ALLOWED_IPS=0.0.0.0                               # allow all IPs
# For now allowing all - change to specific IPs for production
ALLOWED_IPS=0.0.0.0

# ALLOWED_CORS_ORIGINS - Additional allowed CORS origins (comma-separated)
# By default, only port 9040 origins are allowed
# Examples:
#   ALLOWED_CORS_ORIGINS=http://192.168.1.100:9040,https://myserver.local:9040
# ALLOWED_CORS_ORIGINS=

# Restrict admin password reset to specific IPs (comma-separated)
# More restrictive than ALLOWED_IPS - for sensitive reset endpoint only
# Leave unset to follow ALLOWED_IPS setting
# Example: ADMIN_RESET_ALLOWED_IPS=127.0.0.1,192.168.1.100
# ADMIN_RESET_ALLOWED_IPS=

# ==============================================================================
# PRODUCTION CHECKLIST
# ==============================================================================
#
# Before deploying to production, ensure:
# [ ] APP_SECRET_KEY is unique and random (32+ chars)
# [ ] ADMIN_RESET_PIN is strong and unique (8+ chars, not common)
# [ ] APP_ADMIN_PASSWORD meets complexity requirements
# [ ] LOCALHOST_ONLY is set appropriately for your deployment
# [ ] ADMIN_RESET_ALLOWED_IPS is configured if needed
# [ ] All default/placeholder values are replaced
# [ ] .env.local is added to .gitignore (DO NOT commit secrets!)
#
# ==============================================================================
